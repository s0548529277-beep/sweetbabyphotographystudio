בקשה גדולה עם הרבה חלקים. הנה תוכנית מסודרת לביצוע — אנא אשרי לפני שאני מתחיל.

## 1. הסרת חסימת שדות כספיים
- מחיקת הטריגרים `block_client_financial_changes` ו-`block_client_financial_changes_booking` (migration).
- לקוחות יוכלו לערוך/לשלוח הזמנות ללא שגיאת "לא ניתן לשנות שדות כספיים".

## 2. סנכרון דף ניהול
- `admin.orders.tsx` + `admin.calendar.tsx`: שאילתה מאוחדת (orders + bookings) עם `useQuery` + auto-refetch כל 15 שניות.
- כל שינוי סטטוס/מחיר/תאריך מתעדכן בשני המקומות (invalidate query keys משותפים).
- הוספת עמודות חסרות: שם לקוח, טלפון, פריטים, סכום, סטטוס, מקור (props/studio), תאריך.

## 3. תאריך+שעה + חישוב לפי 24 שעות
- שינוי `item_availability`: הוספת `start_at timestamptz`, `end_at timestamptz` (בנוסף לתאריכים לתאימות אחורה).
- `checkout.tsx` + `rental-catalog.tsx`: בורר תאריך **ושעה** התחלה + סיום.
- חישוב מחיר אביזרים: כפולות של 24 שעות (עד 24ש = מחיר בסיס, 25–48ש = כפול, וכו').
- הודעה בולטת: "חובה לעדכן לפני איחור".

## 4. בורר תאריך בראש דף השכרת אביזרים + סימון זמינות על כל פריט
- בראש `/rental-catalog`: date-range picker גדול (מתחייב לבחור לפני צפייה).
- קריאה ל-`checkItemsAvailability` בכל הקטלוג ברגע שבוחרים תאריך.
- כל כרטיס אביזר מציג תג: "זמין" / "תפוס בתאריך שנבחר" (כפתור הוספה מושבת).

## 5. סנכרון קטלוג משולש
- מקור אמת יחיד: טבלת `items` בסופאבייס.
- `rental-catalog`, `checkout`, `admin.items` — כולם קוראים מ-`items` (לא מ-`studio-catalog.json`).
- Seed script שמריץ upsert מ-`studio-catalog.json` → `items` כדי להשלים חוסרים בלי למחוק קיימים.

## 6. חבילות הדרכה
- הוספה ב-`studio-rental.tsx`: "הדרכה טכנית קצרצרה — 50₪" + "הדרכה במקום עד 20 דקות / התאמת סט בסיסי".

## 7. מיילים בסיום הזמנה
- שימוש ב-Lovable Emails (setup_email_infra + scaffold_transactional_email) + דומיין מייל.
- תבנית: אישור הזמנה ללקוח (למייל שנרשם איתו) + עותק ל-`s0548529277@gmail.com`.
- מסך סיום (`/summary`): "נשלח מייל אישור אל …".

## 8. השכרת סטודיו — תוספות
- מלבן תחתון: "צריכה גם אביזרים? עד 20 אביזרים חינם לשעות ההשכרה (~80% מהקטלוג)".
- אופציית "שריון אביזרים" בהמשך הזמנת הסטודיו (multi-select מהקטלוג, שומר ב-`bookings.reserved_items jsonb`).

## 9. עמוד צילומים בסטודיו — גלריית מיכל סיבוני
- סקרייפ מ-`michalsiboni.com`, סינון: לא יותר מ-2 מאותו סט; בלי תמונות ללא אביזרים מהקטלוג.

## 10. שיוך תמונות השראה
- `/rental-catalog`: לכל פריט — לחיצה פותחת מודל עם תמונות השראה (מ-hero-slide-*.jpg + `hasHand:true`).
- `/studio-rental`: כפתור "השראה מהסטודיו" שפותח גלריית תמונות סטודיו.

## 11. עדכון הצ'אטבוט
- קובץ ידע חדש עם: הכללים החדשים, מחירים לפי 24ש, מדיניות איחור, קישור/כפתור לקביעת סטודיו, שאילתת זמינות ("איזה אביזרים זמינים היום?" → קורא ל-`checkItemsAvailability`).
- אינטגרציה עם `ai.functions.ts` (Lovable AI) + כלי function-calling לזמינות.

## הערות טכניות
- כל מיגרציה נפרדת ובעלת אישור מוקדם.
- ה-seed של הקטלוג יעשה `upsert` לפי SKU (מוסיף חסרים, לא מוחק).
- העלייה למיילים דורשת דומיין מאומת — אבקש ממך לפתוח את דיאלוג "הגדרת דומיין מייל" בשלב המתאים.

---

**נא לאשר** ואתחיל בביצוע צעד-אחר-צעד (מיגרציות ראשונות, ואז UI). אם יש חלק שרוצה לדחות/להסיר — כתבי לי לפני האישור.
